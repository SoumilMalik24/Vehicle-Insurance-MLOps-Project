from setuptools import setup, find_packages

#setup() defines metadata and build config
setup(
    name="src",
    version="0.0.1",
    author="Soumil Malik",
    author_email="soumil4malik@gmail.com",
    packages=find_packages() #Auto-discovers packages under the current directory

)